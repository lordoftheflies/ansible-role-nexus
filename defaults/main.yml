---
# roles/nexus/defaults/main.yml

nexus_debug: false

nexus_controller_user: '{{ project_controller_user }}'
nexus_controller_home: '{{ project_controller_home }}'
nexus_controller_resources_root: '{{ project_controller_resources_home }}'

nexus_deployment_user: '{{ project_deployment_user }}'
nexus_deployment_home: '{{ project_deployment_home }}'
nexus_deployment_resources_root: '{{ project_deployment_resources_home }}'

## atomic versioning example
#
# nexus_major: '0'
# nexus_moderate_revision: '0'
# nexus_minor_revision: '1'
# nexus_build: ''

## Package #

nexus_package_state: 'present'
nexus_package_version: '2.0.1'
nexus_package_filename: "vagrant_{{ nexus_package_version }}_i686.deb"

nexus_controller_resources_dir: '{{ nexus_controller_resources_root }}/sw/vagrant/debs'
nexus_deployment_resources_dir: '{{ nexus_deployment_resources_root }}/vagrant/debs'

# /tests/sw/{{ ansible_distribution| lower }}/{{ ansible_distribution_version }}/miniconda'

# Help file

nexus_acemenu_help_file_template_path: '{{ fact_role_path }}/templates/bin/acemenu/help/example_aceadmin_helpfile.md.j2'

# Resources

nexus_package_url: "https://releases.hashicorp.com/vagrant/2.0.1/vagrant_2.0.1_i686.deb"
# nexus_checksum_url: "https://releases.hashicorp.com/vagrant/2.0.1/ vagrant_2.0.1_SHA256SUMS"
# nexus_checksum_sig_url: "https://releases.hashicorp.com/vagrant/2.0.1/ vagrant_2.0.1_SHA256SUMS.sig"

## links

nexus_default_link_force: true

########################################

# Resources

nexus_resources_on_local:

  nexus_local_directories:

    state: 'directory'
    path: '{{ nexus_controller_resources_dir }}'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0755'

  nexus_local_clones:

    become: ''
    repo: 'https://github.com/cjsteel/script-bash-acemenu.git'
    dest: '{{ nexus_controller_resources_dir }}/acemenu'
    mode: '0700'
    clone: true

  nexus_local_downloads:

    url: 'https://repo.continuum.io/miniconda/Miniconda2-4.3.30-Linux-x86_64.sh'
    dest: '{{ nexus_controller_resources_dir }}/Miniconda2-latest-Linux-x86_64.sh'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0700'
    checksum: 'md5:bd1655b4b313f7b2a1f2e15b7b925d03'
    resource_type: 'url'

  nexus_local_file_copies:

    state: "copy"
    src: './files/example_playbook.yml'
    dest: '{{ nexus_controller_resources_dir }}/copy_test.yml'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0700'

  nexus_local_file_touches:

    state: 'touch'
    path: '{{ nexus_controller_resources_dir }}/copy_test.yml'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0770'

  nexus_local_templated_files:


    state: 'template'
    src: './templates/template_test.md.j2'
    dest: '{{ nexus_controller_resources_dir }}/test.md'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0700'

  nexus_local_blocksinfile:

    type: 'blockinfile'
    path: '{{ nexus_controller_resources_dir }}/test.md'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0700'
    insertafter: "EOF"
    block: |
      # if running bash
      if [ -n "$BASH_VERSION" ]; then
          # include /opt/display/bin/display if it exists
          if [ -f "/opt/display/bin/display" ]; then
              PATH="/opt/display/bin:$PATH"

       	. "/opt/display/bin/"
          fi
      fi
    state: "present"

  nexus_local_standard_sym_links:

    state: "link"
    src: '{{ nexus_controller_resources_dir }}/test.md'
    dest: '{{ nexus_controller_resources_dir }}/my_local_link'
    owner: '{{ nexus_controller_user }}'
    group: '{{ nexus_controller_user }}'
    mode: '0700'
    force: '{{ nexus_default_link_force }}'

  nexus_local_relative_sym_links:

    link_name: 'relative_link'
    link_target: 'test.md'
    link_dir: '{{ nexus_controller_resources_dir }}'
    state: 'relative_link'
#    owner: '{{ nexus_controller_user }}'
#    group: '{{ nexus_controller_user }}'
#    mode: '0700'

nexus_resources_on_remote:

  nexus_remote_directories:

    state: 'directory'
    path: '{{ nexus_deployment_resources_dir }}'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0755'

  nexus_remote_clones:

    become: true
    repo: 'https://github.com/cjsteel/script-bash-acemenu.git'
    dest: '{{ nexus_deployment_resources_dir }}/acemenu'
    mode: '0700'
    clone: true

  nexus_remote_downloads:

    url: 'https://repo.continuum.io/miniconda/Miniconda2-4.3.30-Linux-x86_64.sh'
    dest: '{{ nexus_deployment_resources_dir }}/Miniconda2-latest-Linux-x86_64.sh'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'
    checksum: 'md5:bd1655b4b313f7b2a1f2e15b7b925d03'
    resource_type: 'url'

  nexus_remote_file_copies:

    state: "copy"
    src: './files/example_playbook.yml'
    dest: '{{ nexus_deployment_resources_dir }}/copy_test.yml'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'

  nexus_remote_file_touches:

    state: 'touch'
    path: '{{ nexus_deployment_resources_dir }}/copy_test.yml'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0770'

  nexus_remote_templates_files:

    state: 'template'
    src: './templates/template_test.md.j2'
    dest: '{{ nexus_deployment_resources_dir }}/test.md'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'

  nexus_remote_blocksinfile:

    type: 'blockinfile'
    path: '{{ nexus_deployment_resources_dir }}/test.md'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'
    insertafter: "EOF"
    block: |
      # if running bash
      if [ -n "$BASH_VERSION" ]; then
          # include /opt/display/bin/display if it exists
          if [ -f "/opt/display/bin/display" ]; then
              PATH="/opt/display/bin:$PATH"

       	. "/opt/display/bin/"
          fi
      fi
    state: "present"

  nexus_remote_standard_sym_links:

    state: 'link'
    src: '{{ nexus_deployment_resources_dir }}/test.md'
    dest: '{{ nexus_deployment_resources_dir }}/my_remote_link'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'
    force: '{{ nexus_default_link_force }}'

  nexus_remote_relative_sym_links:

    state: 'relative_link'
    link_dir: '{{ nexus_deployment_resources_dir }}'
    link_name: 'my_relative_link'
    link_target: 'test.md'
    owner: '{{ nexus_deployment_user }}'
    group: '{{ nexus_deployment_user }}'
    mode: '0700'
